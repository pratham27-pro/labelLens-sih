// Numeric spacing scale for contexts that need raw numbers (SVG geometry, absolute
// positioning math) rather than NativeWind's p-4 / m-2 style classes.
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};
